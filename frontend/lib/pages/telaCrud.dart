import 'package:flutter/material.dart';

class telaCrud extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Tela de Funções (CRUD)", style: TextStyle(fontSize: 18)),
    );
  }
}
